import { ScaleStripPanel } from "../ScaleStripPanel/ScaleStripPanel";

/** Pure scale reference band — renders only the scale strip. */
export function SummaryRibbon() {
  return <ScaleStripPanel />;
}
