# @fretflow/core

Shared, UI-agnostic music theory and fretboard utilities for FretFlow.

This package intentionally excludes browser, React, CSS, DOM, storage, and audio
runtime code so it can be consumed by the web app and a private Expo mobile app.
