---
phase: 4
plan: "04-03"
title: "Primitive Component A11y — Controls and Panels"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used:
  - ui-design:accessibility-compliance
  - ui-design:web-component-design
  - vercel-react-best-practices
files_modified:
  - src/components/StepperControl.tsx
  - src/components/ToggleBar.tsx
  - src/components/ToggleBar.css
  - src/components/NoteGrid.tsx
  - src/components/FingeringPatternControls.tsx
  - src/components/ScaleChordControls.tsx
  - src/components/shared.css
  - src/__tests__/StepperControl.test.tsx
  - src/__tests__/ToggleBar.test.tsx
  - src/__tests__/NoteGrid.test.tsx
forbidden_commands: []
must_haves:
  truths:
    - "Every screen renders identically — a11y changes are attribute/role additions only, no CSS layout changes"
    - "npm run lint && npm run test && npm run build all pass"
    - "StepperControl decrement/increment buttons have aria-label describing the value being changed"
    - "ToggleBar default variant container has role=group with aria-label"
    - "ToggleBar tabs variant uses role=tablist on container and role=tab + aria-selected on buttons"
    - "NoteGrid container has role=group with aria-label"
    - "FingeringPatternControls shape buttons have aria-pressed reflecting selection state"
    - "ScaleChordControls chord-root section-label is a non-label element (span/p)"
  artifacts:
    - path: "src/components/StepperControl.tsx"
      provides: "aria-label on decrement/increment buttons"
      contains: "aria-label"
    - path: "src/components/ToggleBar.tsx"
      provides: "role=group on default container, role=tablist+tab on tabs variant"
      contains: "role=\"group\""
    - path: "src/components/ToggleBar.tsx"
      provides: "aria-selected on tabs variant buttons"
      contains: "aria-selected"
    - path: "src/components/NoteGrid.tsx"
      provides: "role=group with aria-label on container"
      contains: "role=\"group\""
    - path: "src/components/FingeringPatternControls.tsx"
      provides: "aria-pressed on shape buttons"
      contains: "aria-pressed"
    - path: "src/__tests__/StepperControl.test.tsx"
      provides: "axe assertion for StepperControl"
      contains: "toHaveNoViolations"
    - path: "src/__tests__/ToggleBar.test.tsx"
      provides: "axe assertion for ToggleBar"
      contains: "toHaveNoViolations"
  key_links:
    - from: "src/components/ToggleBar.tsx"
      to: "src/components/MobileTabPanel.tsx"
      via: "ToggleBar tabs variant is consumed by MobileTabPanel — verify no visual regression after tablist change"
    - from: "src/__tests__/StepperControl.test.tsx"
      to: "src/test-utils/a11y.ts"
      via: "imports axe helper from 04-02 tooling plan"
---
<objective>
Add ARIA roles, labels, and keyboard semantics to the generic control primitives (StepperControl, ToggleBar, NoteGrid, FingeringPatternControls, ScaleChordControls). All changes are purely additive attribute/role assignments — no CSS layout, sizing, or visual properties are modified. Add axe-core assertions to existing test files for StepperControl, ToggleBar, and NoteGrid using the vitest-axe helper from plan 04-02.

Key decisions embedded in this plan:
- ToggleBar "tabs" variant: change from aria-pressed to role=tablist/role=tab/aria-selected (semantic correctness, zero visual diff)
- FingeringPatternControls shape buttons: add aria-pressed without routing through ToggleBar (preserves existing Shift+click multi-select logic)
- StepperControl: add descriptive aria-label to each button using the component's existing label prop
- NoteGrid: add role=group + aria-label="Note selector" on the container
- ScaleChordControls: the "Chord Root" section-label that is incorrectly a label element — change to span/p
</objective>
<context>
@.vbw-planning/phases/04-new-features-discovery-and-phase-definition/04-01-RESEARCH.md
@.vbw-planning/phases/04-new-features-discovery-and-phase-definition/.context-lead.md
</context>
<tasks>
<task type="auto">
  <name>Fix StepperControl ARIA — add aria-label to decrement/increment buttons</name>
  <files>
    src/components/StepperControl.tsx
    src/__tests__/StepperControl.test.tsx
  </files>
  <action>
Read src/components/StepperControl.tsx first to understand the component props (label, value, min, max, step, onChange, buttonVariant).

In the decrement button, add:
  aria-label={`Decrease ${label ?? 'value'} (current: ${value})`}

In the increment button, add:
  aria-label={`Increase ${label ?? 'value'} (current: ${value})`}

Also add a role="group" and aria-label on the stepper container div that wraps both buttons and the value display:
  role="group"
  aria-label={label ?? 'Stepper control'}

This gives screen readers: "Fret count, group. Decrease Fret count (current: 5), button. 5. Increase Fret count (current: 5), button."

If src/__tests__/StepperControl.test.tsx does not exist, create it with a basic render test + axe assertion:
  import { render } from '@testing-library/react';
  import { axe } from '../test-utils/a11y';
  import StepperControl from '../components/StepperControl';

  describe('StepperControl', () => {
    it('renders without crashing', () => {
      const { container } = render(<StepperControl label="Fret count" value={5} min={1} max={24} onChange={() => {}} />);
      expect(container).toBeTruthy();
    });
    it('has no a11y violations', async () => {
      const { container } = render(<StepperControl label="Fret count" value={5} min={1} max={24} onChange={() => {}} />);
      expect(await axe(container)).toHaveNoViolations();
    });
  });

If the test file already exists, add the axe assertion test case to the existing describe block.
  </action>
  <verify>
grep -n "aria-label" src/components/StepperControl.tsx
grep -n "toHaveNoViolations" src/__tests__/StepperControl.test.tsx
npm run test -- --reporter=verbose 2>&1 | grep -E "(StepperControl|PASS|FAIL)"
  </verify>
  <done>
First grep shows aria-label on both buttons and the container. Second grep shows axe assertion. Test passes.
  </done>
</task>
<task type="auto">
  <name>Fix ToggleBar ARIA — role=group for default variant, role=tablist/tab for tabs variant</name>
  <files>
    src/components/ToggleBar.tsx
    src/components/ToggleBar.css
    src/__tests__/ToggleBar.test.tsx
  </files>
  <action>
Read src/components/ToggleBar.tsx and src/components/ToggleBar.css first.

Understand the component's variant prop ("default" | "tabs") and its label prop (used for aria-label on the container).

For the DEFAULT variant container div:
- Add role="group"
- Add aria-label={label} (the label prop should already exist; if not, accept it as a new optional prop with a sensible default)
- Keep aria-pressed on individual buttons (correct for toggle-group semantics)

For the TABS variant:
- Change the container div role to role="tablist"
- Add aria-label={label} on the container
- On each button:
  - Change aria-pressed to aria-selected={isActive}
  - Add role="tab"
- Remove aria-pressed from tabs variant buttons

ToggleBar.css: The min-height: 2rem in the tab variant (line ~26) — replace with min-height: calc(2 * var(--space-4)); Only do this if 2rem === 2 * 1rem = var(--space-4) * 2. Check: --space-4 = 1rem, so 2rem = var(--space-8). Replace with min-height: var(--space-8). Confirm the value is exactly 2rem in the file first; if a different value appears, use var(--space-8) only if it equals the exact pixel value.

Note: MobileTabPanel.tsx passes variant="tabs" to ToggleBar. After this change, MobileTabPanel does not need modification — it just benefits from the improved semantics. Verify it still renders correctly by running the existing snapshot test.

If src/__tests__/ToggleBar.test.tsx does not exist, create a minimal test:
  import { render } from '@testing-library/react';
  import { axe } from '../test-utils/a11y';
  import ToggleBar from '../components/ToggleBar';

  describe('ToggleBar', () => {
    it('default variant has no a11y violations', async () => {
      const { container } = render(
        <ToggleBar label="Display mode" options={[{label:'A',value:'a'},{label:'B',value:'b'}]} value="a" onChange={() => {}} />
      );
      expect(await axe(container)).toHaveNoViolations();
    });
    it('tabs variant has no a11y violations', async () => {
      const { container } = render(
        <ToggleBar variant="tabs" label="Panel tabs" options={[{label:'Scale',value:'scale'},{label:'Key',value:'key'}]} value="scale" onChange={() => {}} />
      );
      expect(await axe(container)).toHaveNoViolations();
    });
  });

Adjust the render call to match the actual ToggleBar prop API (read the component first).
  </action>
  <verify>
grep -n "role=\"group\"" src/components/ToggleBar.tsx
grep -n "role=\"tablist\"" src/components/ToggleBar.tsx
grep -n "aria-selected" src/components/ToggleBar.tsx
grep -n "toHaveNoViolations" src/__tests__/ToggleBar.test.tsx
npm run test 2>&1 | tail -5
  </verify>
  <done>
All four greps return matches. Test passes. npm run test exits 0.
  </done>
</task>
<task type="auto">
  <name>Fix NoteGrid and FingeringPatternControls ARIA</name>
  <files>
    src/components/NoteGrid.tsx
    src/components/FingeringPatternControls.tsx
    src/__tests__/NoteGrid.test.tsx
  </files>
  <action>
Read both component files first.

NoteGrid.tsx:
- Find the container element wrapping the note buttons (likely a div with className containing "note-grid").
- Add role="group" and aria-label="Note selector" to the container.
- The individual buttons already have aria-pressed — no change needed there.

FingeringPatternControls.tsx:
- Find the CAGED/3NPS shape buttons (the ones with the TODO comment about Shift+click multi-select, lines ~61–97 in original research).
- Add aria-pressed={selectedShapes.includes(shapeId)} (or equivalent — adapt to the actual data model).
  Read the component to understand how selected state is tracked before writing.
- Add a visible-to-SR description of Shift+click: add aria-describedby pointing to a hidden span that says "Click to select one shape. Shift+click to toggle additional shapes."
  Add the hidden span near the group:
    <span id="fingering-shape-help" className="sr-only">
      Click to select a shape. Shift+click to toggle multiple shapes.
    </span>
  Set aria-describedby="fingering-shape-help" on the container div wrapping the shape buttons.

shared.css: The .sr-only utility class may not exist. Add it if absent:
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }

If src/__tests__/NoteGrid.test.tsx does not exist, create a minimal test:
  import { render } from '@testing-library/react';
  import { axe } from '../test-utils/a11y';
  import NoteGrid from '../components/NoteGrid';

  describe('NoteGrid', () => {
    it('has no a11y violations', async () => {
      const { container } = render(<NoteGrid notes={['C','D','E']} selected="C" onChange={() => {}} />);
      expect(await axe(container)).toHaveNoViolations();
    });
  });

Adjust props to match actual NoteGrid API (read component first).
  </action>
  <verify>
grep -n "role=\"group\"" src/components/NoteGrid.tsx
grep -n "aria-pressed" src/components/FingeringPatternControls.tsx
grep -n "sr-only" src/components/shared.css
grep -n "toHaveNoViolations" src/__tests__/NoteGrid.test.tsx
  </verify>
  <done>
All four greps return matches. npm run test passes.
  </done>
</task>
<task type="auto">
  <name>Fix ScaleChordControls semantic HTML — chord-root label element</name>
  <files>
    src/components/ScaleChordControls.tsx
  </files>
  <action>
Read src/components/ScaleChordControls.tsx first.

Locate the "Chord Root" section-label. Per the Scout audit, it is wrapped in a label element but it does not have a htmlFor pointing to an input — it is purely decorative text labelling the NoteGrid below it.

Change the element from <label ...> to <span ...> or <p ...> with equivalent className. This corrects the invalid HTML (a label element with no associated input). The visual rendering is identical because the styling is CSS-class-driven, not element-type-driven. Verify by checking the CSS for the section-label class — it should apply to both span and label elements.

Also ensure the NoteGrid used for chord root selection (if any) has its own aria-label (covered in the NoteGrid task; no duplication needed here — just verify the NoteGrid receives an appropriate label prop).

Do not change any other elements in this file. No CSS changes.
  </action>
  <verify>
grep -n "Chord Root" src/components/ScaleChordControls.tsx
grep -n "<label" src/components/ScaleChordControls.tsx
  </verify>
  <done>
"Chord Root" section no longer uses a label element without htmlFor. The "Chord Root" text is in a span or p element. npm run lint passes (jsx-a11y/label-has-associated-control no longer warns for this element). npm run build passes.
  </done>
</task>
<task type="auto">
  <name>Run full test suite and validate snapshot stability</name>
  <files>
    src/__tests__/snapshots.test.tsx
  </files>
  <action>
Run npm run test and confirm all tests pass including snapshots.

If any snapshot tests fail due to the ARIA attribute additions in this plan (expected — added role/aria attributes appear in rendered output), update the snapshots intentionally:
  npm run test -- --update-snapshots

Then re-run npm run test to confirm the updated snapshots pass.

Review the snapshot diff carefully: only role, aria-label, aria-selected, aria-pressed attribute additions should appear. No structural DOM changes, no CSS property changes, no element type changes (except the ScaleChordControls label→span fix, which should appear in the diff).

Also run npm run lint and npm run build.
  </action>
  <verify>
npm run test 2>&1 | tail -10
npm run lint 2>&1 | tail -5
npm run build 2>&1 | tail -5
  </verify>
  <done>
npm run test exits 0. npm run lint exits 0. npm run build exits 0. If snapshots were updated, the diff contains only ARIA attribute additions and the label→span change.
  </done>
</task>
</tasks>
<verification>
1. npm run lint -- exits 0
2. npm run test -- exits 0
3. npm run build -- exits 0
4. grep -n "aria-label" src/components/StepperControl.tsx -- returns match on decrement and increment buttons
5. grep -n "role=\"tablist\"" src/components/ToggleBar.tsx -- returns match
6. grep -n "role=\"group\"" src/components/NoteGrid.tsx -- returns match
7. grep -n "aria-pressed" src/components/FingeringPatternControls.tsx -- returns match
8. grep -n "sr-only" src/components/shared.css -- returns match
9. No CAGED color references in StepperControl.tsx or FingeringPatternControls.tsx (those are CSS — covered in 04-01)
</verification>
<success_criteria>
- StepperControl buttons have descriptive aria-label ("Decrease {label} (current: {value})")
- ToggleBar default variant has role=group with aria-label; tabs variant has role=tablist with role=tab and aria-selected on buttons
- NoteGrid container has role=group aria-label="Note selector"
- FingeringPatternControls shape buttons have aria-pressed and a screen-reader hint about Shift+click via aria-describedby
- ScaleChordControls chord-root text is a span/p not an invalid label
- sr-only utility class exists in shared.css
- axe assertions added to StepperControl, ToggleBar, NoteGrid test files and all pass
- All three CI checks pass; no visual regressions in snapshots beyond intended ARIA attribute additions
</success_criteria>
<output>
04-03-SUMMARY.md
</output>

---

## Deviations Reconciled (R01)

### DEV-03: tsconfig.app.json edit

- **Original plan text (files_modified):** Did not include tsconfig.app.json.
- **Actual delivery (commit e7d9b7d):** `tsconfig.app.json` compilerOptions.types was updated to include `"node"` to make `tsc -b` pass given the concurrent 04-05 `primitives-unused.test.ts` file (which uses Node `fs`/`path`/`__dirname` APIs).
- **Rationale:** Cross-wave dependency — 04-05 added a test file that requires Node types; 04-03 happened to be the agent that discovered this during its own test work and applied the fix. Cleaner ownership would have placed this edit in 04-05, but the fix is correct regardless of authoring agent.
- **Resolution:** Future plans that introduce Node-type-requiring test files should own the tsconfig edit. Noted as cross-wave ownership clarification.

### DEV-04: ScaleChordControls chord-root label no-op

- **Original plan text (task target):** "ScaleChordControls chord-root section-label is a non-label element (span/p)."
- **Actual delivery (commit e7d9b7d):** No change was necessary — the chord-root section label was already a `<span>` at the time of the audit. The original Scout audit flagged it for review, but the delivered code matches the intended state.
- **Rationale:** The task bullet was generated from a broad primitive audit; when specific investigation happened, the target was already correct. No regression; no fix needed.
