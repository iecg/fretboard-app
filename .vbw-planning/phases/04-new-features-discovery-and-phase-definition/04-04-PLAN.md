---
phase: 4
plan: "04-04"
title: "SVG and App-Level A11y — CircleOfFifths, FretboardSVG, Help Modal"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used:
  - ui-design:accessibility-compliance
  - ui-design:web-component-design
files_modified:
  - src/CircleOfFifths.tsx
  - src/App.tsx
  - src/App.css
  - src/FretboardSVG.tsx
  - src/__tests__/FretboardSVG.test.tsx
  - src/__tests__/snapshots.test.tsx
forbidden_commands: []
must_haves:
  truths:
    - "Every screen renders identically visually — ARIA/tabIndex additions do not change layout or paint"
    - "npm run lint && npm run test && npm run build all pass"
    - "CircleOfFifths SVG segments are keyboard-navigable via roving tabindex; arrow keys move between the 12 notes"
    - "CircleOfFifths outer SVG has role=group aria-label describing the widget"
    - "FretboardSVG outer SVG has role=img with a dynamic aria-label summarizing what is shown"
    - "FretboardSVG individual note positions do NOT have tabIndex (not keyboard-navigable — NoteGrid is the accessible path)"
    - "App.tsx help modal has a working focus trap and focus restores to the trigger after close"
    - "App.tsx mute button has aria-label (not just title attribute)"
    - "App.css z-index raw values replaced with var(--z-help-overlay), var(--z-note), var(--z-dropdown) tokens from 04-01"
  artifacts:
    - path: "src/CircleOfFifths.tsx"
      provides: "keyboard-navigable CoF with roving tabindex and ARIA"
      contains: "role=\"group\""
    - path: "src/CircleOfFifths.tsx"
      provides: "arrow key handler for roving tabindex"
      contains: "ArrowRight"
    - path: "src/FretboardSVG.tsx"
      provides: "role=img on outer SVG with dynamic aria-label"
      contains: "role=\"img\""
    - path: "src/App.tsx"
      provides: "focus trap in help modal"
      contains: "getFocusableElements"
    - path: "src/App.tsx"
      provides: "aria-label on mute button"
      contains: "aria-label"
    - path: "src/App.css"
      provides: "z-index tokens consumed"
      contains: "var(--z-help-overlay)"
  key_links:
    - from: "src/CircleOfFifths.tsx:roving tabindex"
      to: "src/App.css:.circle-slice:focus-visible"
      via: "existing focus-visible CSS already handles focus ring — tabIndex makes elements reachable"
    - from: "src/App.tsx:help modal focus trap"
      to: "src/components/SettingsOverlay.tsx:getFocusableElements"
      via: "extract getFocusableElements to a shared utility or duplicate the pattern"
    - from: "src/FretboardSVG.tsx:role=img"
      to: "src/__tests__/snapshots.test.tsx"
      via: "snapshot must be updated after role attribute addition"
---
<objective>
Add accessibility support to the two complex SVG components (CircleOfFifths, FretboardSVG) and fix the two App-level a11y gaps (help modal focus trap, mute button aria-label). Also migrate raw z-index values in App.css to the tokens registered in plan 04-01.

Key strategy decisions:
- CircleOfFifths: roving tabindex across 12 segments + arrow key navigation. The existing .circle-slice:focus-visible CSS rule in App.css already provides the focus ring — tabIndex just makes segments reachable. No visual change on unfocused state.
- FretboardSVG: role="img" + dynamic aria-label only. Individual note positions (150+) are NOT made keyboard-navigable — the NoteGrid is the accessible selection path. This is the correct WCAG pattern: provide an alternative mechanism, not keyboard access to every SVG element.
- Help modal: extract or duplicate the getFocusableElements focus-trap pattern from SettingsOverlay. Focus must move to the first focusable element in the modal when it opens and restore to the open trigger on close.
- Mute button: add aria-label={isMuted ? "Unmute audio" : "Mute audio"} (or similar based on actual button label/icon).
</objective>
<context>
@.vbw-planning/phases/04-new-features-discovery-and-phase-definition/04-01-RESEARCH.md
@.vbw-planning/phases/04-new-features-discovery-and-phase-definition/.context-lead.md
</context>
<tasks>
<task type="auto">
  <name>CircleOfFifths — roving tabindex and keyboard navigation</name>
  <files>
    src/CircleOfFifths.tsx
  </files>
  <action>
Read src/CircleOfFifths.tsx in full first. Understand:
- How segments are rendered (likely mapped SVG path/g elements)
- How the selected root is tracked (prop or internal state)
- How onClick is implemented per segment
- The number of segments (12 chromatic notes on the circle)

Implement roving tabindex pattern:
1. Add a state variable to track the currently focused segment index:
   const [focusedIndex, setFocusedIndex] = React.useState<number>(0);

2. On the outer SVG wrapper element, add:
   role="group"
   aria-label="Circle of Fifths — select a key"

3. On each segment element (path, g, or rect — whatever is clickable):
   - Add role="button"
   - Add aria-label={`${noteLabel} — ${isSelected ? 'selected' : 'not selected'}`}
     (noteLabel is the note name for that segment; adapt to actual data model)
   - Add aria-pressed={isSelected} (circle segments behave as radio-button-like toggles; aria-pressed is correct for a single-select clickable element; alternatively use aria-selected if the outer has role="listbox")
   - Add tabIndex={index === focusedIndex ? 0 : -1} (roving tabindex — only one segment is focusable at a time)
   - Add onKeyDown handler for arrow navigation and selection:
     - ArrowRight / ArrowDown: move focusedIndex to (focusedIndex + 1) % 12; focus the next element
     - ArrowLeft / ArrowUp: move focusedIndex to (focusedIndex + 11) % 12; focus the previous element
     - Enter / Space: trigger the same action as onClick for this segment
     - preventDefault() on all handled keys to prevent page scroll

4. When focusedIndex changes, programmatically focus the segment element using a ref array or getElementById:
   Use React.useRef<(SVGElement | null)[]>([]) to store a ref for each segment.
   In a useEffect watching focusedIndex, call segmentRefs.current[focusedIndex]?.focus().

5. When a segment is clicked (onClick), also update focusedIndex to that segment's index so Tab navigation starts from the correct position after a mouse click.

Do not change any visual properties, className, fill, stroke, or inline style attributes. Only add ARIA attributes, tabIndex, and event handlers.
  </action>
  <verify>
grep -n "role=\"group\"" src/CircleOfFifths.tsx
grep -n "role=\"button\"" src/CircleOfFifths.tsx
grep -n "tabIndex" src/CircleOfFifths.tsx
grep -n "ArrowRight" src/CircleOfFifths.tsx
grep -n "aria-pressed" src/CircleOfFifths.tsx
  </verify>
  <done>
All five greps return matches. Component renders without errors (npm run build passes). Visual appearance unchanged (no className or style changes in the diff).
  </done>
</task>
<task type="auto">
  <name>FretboardSVG — role=img with dynamic aria-label</name>
  <files>
    src/FretboardSVG.tsx
    src/__tests__/FretboardSVG.test.tsx
    src/__tests__/snapshots.test.tsx
  </files>
  <action>
Read src/FretboardSVG.tsx first. Understand:
- What props it receives (scale name, root note, chord overlay, tuning, etc.)
- Where the outer SVG element is rendered
- What props are available to construct a meaningful label

On the outermost SVG element:
- Add role="img"
- Add aria-label — construct dynamically from available props. Example:
    aria-label={`Guitar fretboard${rootNote ? ` — ${rootNote}` : ''}${scaleName ? ` ${scaleName}` : ''} scale`}
  Read the actual prop names and adapt. The label should summarize what is visually shown.
- Add a <title> element as the first child of the SVG for additional screen reader support:
    <title>{/* same as aria-label value */}</title>

Do NOT add tabIndex, role="button", or any interaction attributes to individual note positions (<g> elements or circles). The fretboard is a read-only visual representation; selection happens via NoteGrid/CircleOfFifths.

If FretboardSVG.test.tsx exists, add a minimal axe assertion:
  it('has no a11y violations', async () => {
    const { container } = render(<FretboardSVG {...minimalProps} />);
    expect(await axe(container)).toHaveNoViolations();
  });
  (Determine minimalProps by reading the component's prop types.)

After making changes, run the snapshot tests. The snapshot will change because role="img" and the <title> element are new. Update the snapshot intentionally:
  npm run test -- --update-snapshots
Review the diff to confirm only the role, aria-label, and title additions appear.
  </action>
  <verify>
grep -n "role=\"img\"" src/FretboardSVG.tsx
grep -n "<title>" src/FretboardSVG.tsx
grep -n "aria-label" src/FretboardSVG.tsx
npm run test 2>&1 | tail -5
  </verify>
  <done>
All three greps return matches. npm run test passes (snapshots updated). No tabIndex added to note positions (grep -n "tabIndex" src/FretboardSVG.tsx returns no match, or only matches on non-note elements).
  </done>
</task>
<task type="auto">
  <name>App.tsx — help modal focus trap and mute button aria-label</name>
  <files>
    src/App.tsx
  </files>
  <action>
Read src/App.tsx. Find:
1. The help modal (around lines 354–415 per Scout research) — it has role="dialog" and aria-modal="true" but no focus trap.
2. The mute toggle button (around lines 330–341) — it has a title attribute but no aria-label.

MUTE BUTTON FIX:
Find the mute button element. Add:
  aria-label={isMuted ? 'Unmute audio' : 'Mute audio'}
(Adapt to the actual state variable name. Read the component first.)
Remove the title attribute or keep it as a tooltip supplement — aria-label is the primary screen reader text.

HELP MODAL FOCUS TRAP:
The SettingsOverlay.tsx has a working focus trap implementation using a getFocusableElements utility. Options:
a) If SettingsOverlay exports getFocusableElements as a named export, import it in App.tsx.
b) If it does not export it, extract the utility to src/utils/focus.ts and update the import in SettingsOverlay.tsx.
c) If extraction is complex, duplicate the 5-line getFocusableElements inline in App.tsx.

Implement the focus trap in the help modal:
1. Create a ref for the help modal container: const helpModalRef = React.useRef<HTMLDivElement>(null);
2. Create a ref for the open trigger (help button): const helpTriggerRef = React.useRef<HTMLButtonElement>(null);
3. When the help modal opens (isHelpOpen state goes true):
   - In a useEffect, focus the first focusable element inside helpModalRef.current.
4. When the help modal closes (isHelpOpen goes false):
   - In the useEffect cleanup or a dedicated effect, restore focus to helpTriggerRef.current.
5. Add a keydown handler on the modal container that traps Tab/Shift+Tab within getFocusableElements(helpModalRef.current) — same pattern as SettingsOverlay.

Do not change any visual elements, layout, or CSS in App.tsx. Only add refs, effects, and the aria-label.
  </action>
  <verify>
grep -n "aria-label" src/App.tsx
grep -n "getFocusableElements\|focus-trap\|helpModalRef" src/App.tsx
npm run build 2>&1 | tail -5
  </verify>
  <done>
First grep returns match on the mute button's aria-label. Second grep returns match for focus trap implementation. npm run build passes with no TypeScript errors.
  </done>
</task>
<task type="auto">
  <name>App.css — migrate z-index raw values to tokens</name>
  <files>
    src/App.css
  </files>
  <action>
Read src/App.css. Find the following raw z-index values identified in the Scout research and replace them with the tokens registered in plan 04-01:

1. z-index: 1000 (help overlay, line ~235) → z-index: var(--z-help-overlay)
2. z-index: 10 (note z-ordering, line ~467) → z-index: var(--z-note)
3. z-index: 50 (drawer-options, line ~1287) → z-index: var(--z-dropdown)

Also check for z-index: 1 (if present in App.css) — replace with z-index: 1 (no token needed; layer 1 is not registered as a named concern).

These substitutions are value-identical replacements — the token values defined in semantic.css are 1000, 10, and 50 respectively. Zero visual change.

Add a comment above each replaced rule:
  /* z-index token — see semantic.css for the full z-index registry */

Do not change any other values in App.css. Do not migrate spacing or font-size values (those are Phase 5 scope per the phase constraints).
  </action>
  <verify>
grep -n "var(--z-help-overlay)" src/App.css
grep -n "var(--z-note)" src/App.css
grep -n "var(--z-dropdown)" src/App.css
grep -c "z-index: 1000" src/App.css
grep -c "z-index: 50" src/App.css
  </verify>
  <done>
First three greps return matches. Last two greps return 0 (raw values replaced). npm run build passes.
  </done>
</task>
<task type="auto">
  <name>Run full test suite and validate all changes</name>
  <files>
    src/__tests__/snapshots.test.tsx
  </files>
  <action>
Run the complete test suite:
  npm run test

If snapshot tests fail due to the role="img", <title>, or ARIA attribute additions in FretboardSVG or CircleOfFifths, update them:
  npm run test -- --update-snapshots

Re-run npm run test to confirm the updated snapshots pass.

Review the full snapshot diff. Acceptable changes:
- role="img" attribute on FretboardSVG outer SVG
- <title>...</title> element added inside FretboardSVG SVG
- aria-label attribute on FretboardSVG SVG
- role="group", role="button", aria-label, aria-pressed, tabIndex attributes on CircleOfFifths elements

Unacceptable changes that would indicate a regression:
- Any change to fill, stroke, d, cx, cy, r, width, height, x, y attributes (SVG geometry)
- Any change to className on existing elements
- Any DOM structural change (elements added/removed beyond the <title> element)

Run npm run lint and npm run build. All three checks must exit 0.
  </action>
  <verify>
npm run test 2>&1 | tail -10
npm run lint 2>&1 | tail -5
npm run build 2>&1 | tail -5
  </verify>
  <done>
All three commands exit 0. Snapshot diffs contain only ARIA/role/tabIndex/title additions. No geometry or className changes appear.
  </done>
</task>
</tasks>
<verification>
1. npm run lint -- exits 0
2. npm run test -- exits 0 (snapshots updated where needed)
3. npm run build -- exits 0
4. grep -n "role=\"group\"" src/CircleOfFifths.tsx -- returns match (outer SVG)
5. grep -n "role=\"button\"" src/CircleOfFifths.tsx -- returns match (segment elements)
6. grep -n "ArrowRight" src/CircleOfFifths.tsx -- returns match (keyboard handler)
7. grep -n "role=\"img\"" src/FretboardSVG.tsx -- returns match
8. grep -n "aria-label" src/App.tsx -- returns match (mute button)
9. grep -n "getFocusableElements\|helpModalRef" src/App.tsx -- returns match (focus trap)
10. grep -n "var(--z-help-overlay)" src/App.css -- returns match
11. grep -c "z-index: 1000" src/App.css -- returns 0
</verification>
<success_criteria>
- CircleOfFifths: 12 SVG segments keyboard-navigable via roving tabindex; arrow keys cycle through notes; Enter/Space triggers selection; outer SVG has role=group aria-label
- FretboardSVG: outer SVG has role=img with dynamic aria-label and SVG title element; individual note positions have NO tabIndex (NoteGrid is the accessible path)
- App.tsx: help modal has working focus trap (Tab stays within modal, focus restores to trigger on close); mute button has aria-label replacing title-only
- App.css: z-index 1000/10/50 replaced with var(--z-help-overlay)/var(--z-note)/var(--z-dropdown)
- Snapshot tests updated with only ARIA/role/title additions — no geometry or className regressions
- All three CI checks (lint, test, build) pass
</success_criteria>
<output>
04-04-SUMMARY.md
</output>

---

## Deviations Reconciled (R01)

### DEV-05: FretboardSVG role=img on wrapper div

- **Original plan text (must_haves.truths):** "FretboardSVG outer SVG has role=img with a dynamic aria-label summarizing what is shown."
- **Actual delivery (commit e7d9b7d):** FretboardSVG renders a React fragment (multiple top-level SVG elements, no single outer wrapper), so `role="img"` was added to a `<div>` wrapping the fragment rather than an `<svg>` element.
- **Rationale:** The original plan text assumed FretboardSVG had a single outer `<svg>` element. It does not — the component returns `<>{...}</>` for layout flexibility in the parent. The correct WCAG 2.1 authoring pattern for "this group of visual content represents one coherent image" is a labeling wrapper element with `role="img"`. A div wrapper is semantically valid and does not change visual rendering.
- **Snapshot impact:** The wrapper div appears in snapshot tests. Snapshots were updated intentionally in commit a48b314.
