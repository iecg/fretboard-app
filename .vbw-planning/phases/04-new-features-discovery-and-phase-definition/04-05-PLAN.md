---
phase: 4
plan: "04-05"
title: "Phase 5-Enablement Primitives (Card, AppHeader, DegreeChipStrip, BottomTabBar, LabeledSelect)"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used:
  - ui-design:design-system-patterns
  - ui-design:web-component-design
  - ui-design:create-component
  - ui-design:accessibility-compliance
  - vercel-react-best-practices
files_modified:
  - src/components/Card.tsx
  - src/components/Card.css
  - src/components/AppHeader.tsx
  - src/components/AppHeader.css
  - src/components/DegreeChipStrip.tsx
  - src/components/DegreeChipStrip.css
  - src/components/BottomTabBar.tsx
  - src/components/BottomTabBar.css
  - src/components/LabeledSelect.tsx
  - src/components/LabeledSelect.css
  - src/__tests__/Card.test.tsx
  - src/__tests__/AppHeader.test.tsx
  - src/__tests__/DegreeChipStrip.test.tsx
  - src/__tests__/BottomTabBar.test.tsx
  - src/__tests__/LabeledSelect.test.tsx
forbidden_commands: []
must_haves:
  truths:
    - "Every existing screen renders byte-identically — no new primitive is imported by any non-test file in src/ outside the new primitive files themselves"
    - "All 5 new primitives consume tokens exclusively — no hardcoded colors, spacing, radius, shadow, font-weight, or z-index values in the new CSS files (exceptions only for animation timing and cubic-bezier curves)"
    - "Every primitive has a vitest-axe test asserting toHaveNoViolations() for its default render and one interactive state"
    - "Every primitive ships with typed props (TypeScript — no any, no unknown unless intentional)"
    - "Keyboard navigation works as documented in each primitive section below"
    - "npm run lint && npm run test && npm run build all pass"
  artifacts:
    - path: "src/components/Card.tsx"
      provides: "reusable card surface with header + body slots"
      contains: "export function Card"
    - path: "src/components/Card.tsx"
      provides: "composable sub-components"
      contains: "Card.Header"
    - path: "src/components/AppHeader.tsx"
      provides: "brand + actions header bar"
      contains: "export function AppHeader"
    - path: "src/components/DegreeChipStrip.tsx"
      provides: "scale degree visualization primitive"
      contains: "export function DegreeChipStrip"
    - path: "src/components/BottomTabBar.tsx"
      provides: "mobile bottom nav shell"
      contains: "export function BottomTabBar"
    - path: "src/components/LabeledSelect.tsx"
      provides: "compact label-above-field dropdown"
      contains: "export function LabeledSelect"
    - path: "src/__tests__/Card.test.tsx"
      provides: "axe assertion for Card"
      contains: "toHaveNoViolations"
    - path: "src/__tests__/AppHeader.test.tsx"
      provides: "axe assertion for AppHeader"
      contains: "toHaveNoViolations"
    - path: "src/__tests__/DegreeChipStrip.test.tsx"
      provides: "axe assertion for DegreeChipStrip"
      contains: "toHaveNoViolations"
    - path: "src/__tests__/BottomTabBar.test.tsx"
      provides: "axe assertion for BottomTabBar"
      contains: "toHaveNoViolations"
    - path: "src/__tests__/LabeledSelect.test.tsx"
      provides: "axe assertion for LabeledSelect"
      contains: "toHaveNoViolations"
  key_links:
    - from: "src/components/Card.css"
      to: "src/semantic.css:--surface-card"
      via: "consumes Phase-5 card surface tokens from 04-01"
    - from: "src/components/DegreeChipStrip.css"
      to: "src/semantic.css:--chip-surface-active"
      via: "consumes Phase-5 chip tokens from 04-01"
    - from: "src/components/AppHeader.css"
      to: "src/semantic.css:--header-surface"
      via: "consumes Phase-5 header tokens from 04-01"
    - from: "src/__tests__/*.test.tsx"
      to: "src/test-utils/a11y.ts"
      via: "imports axe matcher installed by 04-02"
---
<objective>
Deliver five Phase 5-enablement primitive components that encode the visual language shown in the Phase 5 mockups (see 05-CONTEXT.md). These primitives ship as foundation — fully implemented, tokenized, tested, and a11y-clean — but NOT wired into any existing screen in Phase 4. Phase 5 consumes them when it restructures the app layout.

All primitives must:
1. Consume tokens added by plan 04-01 (neon palette, glow, card surfaces, chip surfaces, header surface, radius, elevation, display font, etc.). No hardcoded visual values.
2. Be keyboard-accessible and screen-reader-correct (WCAG 2.1 AA).
3. Ship with a vitest-axe test (via helpers installed by 04-02).
4. Use TypeScript with explicit prop types.
5. Follow project conventions: clsx for conditional classes, cva if variant systems emerge, motion/react for animations where appropriate.
6. Live in src/components/ using the file-per-component pattern already established.
</objective>
<context>
@.vbw-planning/phases/04-new-features-discovery-and-phase-definition/04-01-RESEARCH.md
@.vbw-planning/phases/05-ui-application-visual-mobile-onboarding/05-CONTEXT.md
@CLAUDE.md

The five primitives trace directly to the Phase 5 mockups:
- Card → the 3-column desktop panels (Configuration, Music Theory, Key Explorer)
- AppHeader → the brand + actions row with a clear bottom separator
- DegreeChipStrip → the top strip showing scale name and degree pills
- BottomTabBar → the mobile Fretboard / Chords / Library / Profile nav
- LabeledSelect → the tight "label above field" dropdown pattern (Scale Family)

Depends on 04-01 (tokens) and 04-02 (a11y test tooling). Phase 4 leaves these primitives unconsumed — Phase 5 imports them.
</context>
<tasks>
<task type="auto">
  <name>Implement Card primitive (surface + header + body slots)</name>
  <files>
    src/components/Card.tsx
    src/components/Card.css
    src/__tests__/Card.test.tsx
  </files>
  <action>
Read .vbw-planning/phases/05-ui-application-visual-mobile-onboarding/05-CONTEXT.md for the full Phase 5 vision. Read src/semantic.css (post-04-01) to confirm --surface-card, --surface-card-border, --surface-card-header-border, --elevation-card, --radius-card, --card-padding-x, --card-padding-y are defined.

Implement Card as a compound component:

```tsx
// src/components/Card.tsx
import { type ReactNode } from 'react';
import clsx from 'clsx';
import './Card.css';

export interface CardProps {
  title?: string;
  icon?: ReactNode;
  action?: ReactNode;       // right-aligned action slot (e.g., button, link)
  children: ReactNode;
  className?: string;
  as?: 'section' | 'article' | 'div';
  'aria-labelledby'?: string;
}

export function Card({ title, icon, action, children, className, as: As = 'section', ...rest }: CardProps) {
  // If title is provided and no aria-labelledby passed, generate one for a11y
  // Otherwise require caller to set aria-label or aria-labelledby
  ...
}

Card.Header = function CardHeader({ title, icon, action, id }) { ... };
Card.Body = function CardBody({ children, className }) { ... };
```

Requirements:
- Root element renders as `<section>` by default (semantic region); `as` prop allows article/div.
- When `title` is provided, the header renders with the title inside `<h2>` (use `var(--font-weight-semibold)`, `var(--text-lg)`). The section element gets `aria-labelledby={generatedId}` pointing at the h2's id.
- When `title` is NOT provided and no `aria-label`/`aria-labelledby` is passed, console.warn in dev (not in prod) about missing accessible name.
- `icon` renders left of title (use `var(--icon-md)` sizing).
- `action` renders right of title, aligned to the end.
- Divider between header and body uses `border-bottom: 1px solid var(--surface-card-header-border)`.
- Body padding uses `var(--card-padding-y) var(--card-padding-x)`.
- Card has `border-radius: var(--radius-card)`, `border: 1px solid var(--surface-card-border)`, `background: var(--surface-card)`, `box-shadow: var(--elevation-card)`.
- Support `className` pass-through for layout positioning by callers.

Write Card.test.tsx:
- Renders with title — asserts h2 exists, aria-labelledby on section matches h2 id
- Renders Card.Header and Card.Body composition path
- Renders with `action` slot
- toHaveNoViolations assertion (imported from src/test-utils/a11y.ts)
- Throws dev warning when no title and no aria-label (vi.spyOn console.warn)

No hardcoded colors, spacing, radius, shadow, or font-weight values in Card.css.
  </action>
  <verify>
test -f src/components/Card.tsx
test -f src/components/Card.css
test -f src/__tests__/Card.test.tsx
grep -n "export function Card" src/components/Card.tsx
grep -n "Card.Header" src/components/Card.tsx
grep -n "toHaveNoViolations" src/__tests__/Card.test.tsx
grep -Pn "#[0-9a-fA-F]{3,8}|\\b(px|rem|em)\\b" src/components/Card.css | grep -v "0px\|0rem\|transparent" || echo "OK: no hardcoded values"
  </verify>
  <done>
All 4 artifacts exist. Component exports match. No hardcoded color/spacing values in CSS (except possibly 0 values). npx vitest run Card passes.
  </done>
</task>
<task type="auto">
  <name>Implement AppHeader primitive (brand + actions + separator)</name>
  <files>
    src/components/AppHeader.tsx
    src/components/AppHeader.css
    src/__tests__/AppHeader.test.tsx
  </files>
  <action>
Read .vbw-planning/phases/05-ui-application-visual-mobile-onboarding/05-CONTEXT.md for the brand mockup reference. Read src/semantic.css (post-04-01) for --header-surface, --header-border, --header-padding-y, --text-brand, --font-display, --brand-icon-glow.

Implement:

```tsx
// src/components/AppHeader.tsx
import { type ReactNode } from 'react';
import './AppHeader.css';

export interface AppHeaderProps {
  brandTitle: string;
  brandSubtitle?: string;
  brandIcon?: ReactNode;       // SVG icon slot (e.g., guitar glyph)
  actions?: ReactNode;         // right-aligned action slot (buttons, user menu)
  className?: string;
}

export function AppHeader({ brandTitle, brandSubtitle, brandIcon, actions, className }: AppHeaderProps) {
  // <header role="banner"> with brand on left, actions on right, separator below
}
```

Requirements:
- Root element is `<header role="banner">`.
- Brand title uses `font-family: var(--font-display)`, `font-weight: var(--font-brand-weight)`, `color: var(--text-brand)`. If `--text-brand-gradient` is set (Phase 5 future), the Lead may use `background: var(--text-brand-gradient); background-clip: text; color: transparent;` — but for Phase 4, the simple solid color is fine (gradient is a Phase 5 enhancement).
- Brand icon slot receives `filter: var(--brand-icon-glow)` to add the neon glow effect.
- Subtitle uses `color: var(--text-muted)`, `font-size: var(--text-sm)`.
- Actions slot is right-aligned, vertically centered, gap between items uses `var(--space-2)`.
- Bottom border uses `border-bottom: 1px solid var(--header-border)` — this is the "clear header bar with separation" the user called out.
- Padding uses `var(--header-padding-y) var(--space-4)`.
- Background uses `var(--header-surface)`.
- `role="banner"` + any action buttons must have accessible names.

Write AppHeader.test.tsx:
- Renders with brandTitle — asserts `<header>` with role="banner" exists
- Renders brandSubtitle when provided
- Renders actions slot
- Renders brandIcon slot
- toHaveNoViolations assertion
- When actions contain buttons, verifies they are keyboard-focusable (Tab navigation)
  </action>
  <verify>
test -f src/components/AppHeader.tsx
test -f src/components/AppHeader.css
test -f src/__tests__/AppHeader.test.tsx
grep -n "export function AppHeader" src/components/AppHeader.tsx
grep -n "role=\"banner\"" src/components/AppHeader.tsx
grep -n "toHaveNoViolations" src/__tests__/AppHeader.test.tsx
  </verify>
  <done>
Artifacts exist, exports correct, test passes, role="banner" present. No hardcoded visual values in AppHeader.css.
  </done>
</task>
<task type="auto">
  <name>Implement DegreeChipStrip primitive (scale degree visualization)</name>
  <files>
    src/components/DegreeChipStrip.tsx
    src/components/DegreeChipStrip.css
    src/__tests__/DegreeChipStrip.test.tsx
  </files>
  <action>
Read .vbw-planning/phases/05-ui-application-visual-mobile-onboarding/05-CONTEXT.md — specifically the "A Natural Minor (Aeolian) (6th Mode)" chip strip example. Read src/semantic.css (post-04-01) for --chip-surface-{inactive,active,tonic}, --chip-border-{inactive,active,tonic}, --chip-text-{inactive,active,tonic}, --radius-pill.

Implement:

```tsx
// src/components/DegreeChipStrip.tsx
import { type ReactNode } from 'react';
import clsx from 'clsx';
import './DegreeChipStrip.css';

export interface DegreeChip {
  note: string;         // "A", "B", "C#", etc.
  interval: string;     // "1", "b3", "4", "#5", etc. — displayed below note
  inScale: boolean;     // true if this degree is part of the active scale
  isTonic?: boolean;    // true for the 1 degree
  inChord?: boolean;    // true if this degree is part of the active chord overlay
}

export interface DegreeChipStripProps {
  scaleName: string;        // e.g., "A Natural Minor (Aeolian) (6th Mode)"
  chips: DegreeChip[];
  className?: string;
  'aria-label'?: string;    // defaults to `Scale degrees for {scaleName}`
}

export function DegreeChipStrip({ scaleName, chips, className, ...rest }: DegreeChipStripProps) {
  // <section role="group" aria-label="...">
  //   <header>{scaleName}</header>
  //   <ul role="list">
  //     {chips.map(...)}
  //   </ul>
  // </section>
}
```

Requirements:
- Root is `<section role="group">` with aria-label.
- Scale name renders inside `<header>` using `var(--font-weight-medium)`, `var(--text-sm)`, `var(--text-secondary)`.
- Chips render as an `<ul role="list">` with `<li>` items (not buttons — this primitive is display-only; Phase 5 or later can make them interactive).
- Each chip renders note on top line, interval on bottom line. Note uses display font (`var(--font-display)`, `var(--font-weight-semibold)`, `var(--text-base)`); interval uses `var(--text-xs)`, `var(--text-muted)`.
- Chip states:
  - default (inScale=false): surface=inactive, border=inactive, text=inactive; opacity 0.55
  - inScale=true: surface=active, border=active (1px solid --chip-border-active), text=active, shadow=var(--glow-cyan-sm)
  - isTonic=true: surface=tonic, border=tonic, text=tonic, shadow=var(--glow-orange-sm)
  - inChord=true: surface=tonic (chord notes use the orange/chord emphasis), same treatment as tonic
- Connector lines between chips (the thin line visible in the mockup between circle chips) use `::after` pseudo-element with `background: var(--chip-border-inactive)`, `height: 1px`, horizontally spanning the gap — NOT rendered for the last chip.
- `border-radius: var(--radius-pill)` on each chip circle.
- Chip sizes: min width 2.5rem, padding `var(--space-2) var(--space-3)`, gap between chips `var(--space-3)`.

Write DegreeChipStrip.test.tsx:
- Renders with 7 chips — asserts role=group with aria-label
- Asserts role=list with 7 listitems
- Asserts tonic chip has correct data attribute or class for styling verification
- Asserts in-scale chips have different class/state than out-of-scale
- toHaveNoViolations assertion

Use data attributes (e.g., `data-in-scale`, `data-is-tonic`, `data-in-chord`) to drive the CSS state selectors — keeps the component's JSX clean and testable.
  </action>
  <verify>
test -f src/components/DegreeChipStrip.tsx
test -f src/components/DegreeChipStrip.css
test -f src/__tests__/DegreeChipStrip.test.tsx
grep -n "export function DegreeChipStrip" src/components/DegreeChipStrip.tsx
grep -n "role=\"group\"" src/components/DegreeChipStrip.tsx
grep -n "toHaveNoViolations" src/__tests__/DegreeChipStrip.test.tsx
  </verify>
  <done>
Artifacts exist, exports correct, test passes, a11y roles present. No hardcoded visual values in DegreeChipStrip.css.
  </done>
</task>
<task type="auto">
  <name>Implement BottomTabBar primitive (mobile nav shell)</name>
  <files>
    src/components/BottomTabBar.tsx
    src/components/BottomTabBar.css
    src/__tests__/BottomTabBar.test.tsx
  </files>
  <action>
Read .vbw-planning/phases/05-ui-application-visual-mobile-onboarding/05-CONTEXT.md for the mobile mockup — bottom tab bar shows Fretboard / Chords / Library / Profile with icons above labels. Read src/semantic.css (post-04-01) for --z-bottom-nav, --header-surface (same surface token reused), --header-border.

Implement:

```tsx
// src/components/BottomTabBar.tsx
import { type ReactNode } from 'react';
import clsx from 'clsx';
import './BottomTabBar.css';

export interface BottomTabItem {
  id: string;
  label: string;
  icon: ReactNode;          // SVG icon node
  badge?: number | string;  // optional notification badge
  disabled?: boolean;
}

export interface BottomTabBarProps {
  items: BottomTabItem[];
  activeId: string;
  onSelect: (id: string) => void;
  'aria-label'?: string;    // defaults to 'Primary navigation'
  className?: string;
}

export function BottomTabBar({ items, activeId, onSelect, ...rest }: BottomTabBarProps) {
  // <nav role="navigation" aria-label="...">
  //   <ul role="tablist">
  //     {items.map(item => <button role="tab" aria-selected={...} ... />)}
  //   </ul>
  // </nav>
}
```

Requirements:
- Root is `<nav aria-label>` (role=navigation implicit).
- Each tab is a `<button>` (not anchor — Phase 5 wires real routing; this is a state-driven selector).
- Use `role="tablist"` + `role="tab"` + `aria-selected` pattern.
- Arrow key navigation: ArrowLeft/ArrowRight moves focus between tabs with roving tabindex (only active tab is `tabIndex={0}`, others `tabIndex={-1}`). Home/End jumps to first/last.
- Activate on Space or Enter (native button behavior handles this).
- Active tab: text uses `var(--neon-cyan-bright)`, has top indicator bar using `var(--neon-cyan)` with `box-shadow: var(--glow-cyan-sm)`.
- Inactive tab: text uses `var(--text-muted)`.
- Icon size: `var(--icon-md)`.
- Label font: `var(--text-xs)`, `var(--font-weight-medium)`.
- Position: `position: fixed; bottom: 0; left: 0; right: 0`. Z-index: `var(--z-bottom-nav)`.
- Background: `var(--header-surface)` with `border-top: 1px solid var(--header-border)` + `backdrop-filter: blur(8px)` for the glass effect.
- Padding bottom includes `env(safe-area-inset-bottom)` for iOS.
- Height ~64px using tokens (`calc(var(--space-5) * 2 + var(--icon-md) + var(--text-xs-line-height))` or simply var(--size-touch-target) + padding).
- Badge renders as small pill in top-right of tab using `var(--neon-orange)` bg and white text; `var(--text-xs)`.

Write BottomTabBar.test.tsx:
- Renders with 4 items — asserts nav, role=tablist, 4 role=tab buttons
- Clicking a tab calls onSelect with item.id
- Arrow key navigation: right arrow moves focus and triggers onSelect for next tab (simulate via userEvent)
- Home/End navigation works
- aria-selected reflects activeId
- toHaveNoViolations assertion
- Disabled item does not receive focus and does not call onSelect

IMPORTANT: Phase 5 wires routing. Phase 4 ships this as a pure controlled component (state managed by parent via props). Do NOT hardcode any route paths.
  </action>
  <verify>
test -f src/components/BottomTabBar.tsx
test -f src/components/BottomTabBar.css
test -f src/__tests__/BottomTabBar.test.tsx
grep -n "export function BottomTabBar" src/components/BottomTabBar.tsx
grep -n "role=\"tab\"" src/components/BottomTabBar.tsx
grep -n "ArrowRight" src/components/BottomTabBar.tsx
grep -n "toHaveNoViolations" src/__tests__/BottomTabBar.test.tsx
  </verify>
  <done>
Artifacts exist. Keyboard nav verified. a11y roles present. No hardcoded values in CSS.
  </done>
</task>
<task type="auto">
  <name>Implement LabeledSelect primitive (compact label-above-field pattern)</name>
  <files>
    src/components/LabeledSelect.tsx
    src/components/LabeledSelect.css
    src/__tests__/LabeledSelect.test.tsx
  </files>
  <action>
Read .vbw-planning/phases/05-ui-application-visual-mobile-onboarding/05-CONTEXT.md — Scale Family dropdown example with "Scale Family" label above "Major Modes" value. Read src/DrawerSelector.tsx to understand the existing drawer pattern (LabeledSelect can either wrap DrawerSelector or implement its own compact variant).

Decision tree (Lead chooses):
- If DrawerSelector already supports a `label` prop or can be trivially extended: wrap it.
- If DrawerSelector is too opinionated for this compact inline pattern: implement LabeledSelect as a fresh primitive using native `<select>` under the hood with visual override (more accessible, less JS), OR a fresh button+listbox pattern.

Recommendation: implement fresh with native `<select>` — the mockup shows a simple single-select with a chevron. Native select is the most accessible path and avoids reimplementing keyboard navigation.

Implement:

```tsx
// src/components/LabeledSelect.tsx
import { type ChangeEvent, useId } from 'react';
import clsx from 'clsx';
import './LabeledSelect.css';

export interface LabeledSelectOption {
  value: string;
  label: string;
  disabled?: boolean;
}

export interface LabeledSelectProps {
  label: string;
  value: string;
  options: LabeledSelectOption[];
  onChange: (value: string) => void;
  id?: string;
  className?: string;
  'aria-describedby'?: string;
  disabled?: boolean;
}

export function LabeledSelect({ label, value, options, onChange, id, className, ...rest }: LabeledSelectProps) {
  const generatedId = useId();
  const selectId = id ?? generatedId;
  return (
    <div className={clsx('labeled-select', className)}>
      <label htmlFor={selectId}>{label}</label>
      <div className="labeled-select-field">
        <select id={selectId} value={value} onChange={e => onChange(e.target.value)} {...rest}>
          {options.map(o => <option key={o.value} value={o.value} disabled={o.disabled}>{o.label}</option>)}
        </select>
        <ChevronIcon aria-hidden />
      </div>
    </div>
  );
}
```

Requirements:
- Real `<label>` element with `htmlFor` linking to select id (proper a11y).
- Label uses `var(--text-xs)`, `var(--text-muted)`, `var(--font-weight-medium)`. Displayed ABOVE the field with `var(--space-1)` gap.
- Field styled to match the mockup: dark surface (use `var(--surface-card)` or `var(--color-surface-1)`), 1px border `var(--surface-card-border)`, radius `var(--radius-md)` (or `--radius-sm`), padding `var(--space-2) var(--space-3)`.
- Select reset: `appearance: none`, then custom chevron positioned absolutely right. Chevron color: `var(--text-muted)`.
- Focus ring: `outline: var(--focus-ring); outline-offset: var(--focus-ring-offset)` on `:focus-visible`.
- Font: `var(--font-sans)`, `var(--text-sm)`, `var(--font-weight-medium)`, color `var(--text-primary)`.
- Disabled state: `opacity: var(--disabled-opacity)`.

Write LabeledSelect.test.tsx:
- Renders with label and options — asserts label text, select has matching id
- Changing selection fires onChange with new value
- Disabled prop disables the select
- aria-describedby pass-through works
- toHaveNoViolations assertion
  </action>
  <verify>
test -f src/components/LabeledSelect.tsx
test -f src/components/LabeledSelect.css
test -f src/__tests__/LabeledSelect.test.tsx
grep -n "export function LabeledSelect" src/components/LabeledSelect.tsx
grep -n "htmlFor" src/components/LabeledSelect.tsx
grep -n "toHaveNoViolations" src/__tests__/LabeledSelect.test.tsx
  </verify>
  <done>
Artifacts exist. Native select pattern. label/select linked. a11y test passes.
  </done>
</task>
<task type="auto">
  <name>Verify no primitive is consumed outside its own files and write phase guardrail test</name>
  <files>
    src/__tests__/primitives-unused.test.ts
  </files>
  <action>
Write a test that guarantees the Phase 4 "defined but unused" contract: each new primitive is NOT imported by any file in src/ except its own test file. Phase 5 will remove or update this test when it wires the primitives in.

```ts
// src/__tests__/primitives-unused.test.ts
import { describe, it, expect } from 'vitest';
import { readFileSync, readdirSync } from 'fs';
import { join } from 'path';

const primitives = ['Card', 'AppHeader', 'DegreeChipStrip', 'BottomTabBar', 'LabeledSelect'];
const srcDir = 'src';

function walk(dir: string): string[] {
  const entries = readdirSync(dir, { withFileTypes: true });
  return entries.flatMap(e => {
    const p = join(dir, e.name);
    if (e.isDirectory()) return walk(p);
    if (!/\.(tsx?|jsx?)$/.test(e.name)) return [];
    return [p];
  });
}

describe('Phase 4 primitive isolation', () => {
  const files = walk(srcDir);

  for (const primitive of primitives) {
    it(`${primitive} is defined but not yet consumed by app code`, () => {
      const importPattern = new RegExp(`(from\\s+['"][^'"]*${primitive}['"]|import\\s+.*${primitive}.*from)`);
      const consumers = files.filter(f => {
        // Skip the primitive's own .tsx
        if (f.endsWith(`${primitive}.tsx`)) return false;
        // Skip its own test
        if (f.endsWith(`${primitive}.test.tsx`)) return false;
        const content = readFileSync(f, 'utf8');
        return importPattern.test(content);
      });
      expect(consumers, `${primitive} imported by: ${consumers.join(', ')}`).toEqual([]);
    });
  }
});
```

This test fails loudly if someone accidentally wires a Phase 5 primitive into a Phase 4 screen. Phase 5 will delete this file when wiring begins.

Add a header comment to the test explaining its purpose and when to delete it:

```ts
/**
 * Phase 4 guardrail: Verifies that new primitives shipped in plan 04-05 are NOT yet
 * consumed by any app code. They exist as foundation only until Phase 5 wires them in.
 *
 * DELETE THIS FILE when starting Phase 5 (UI Application). Phase 5's first plan should
 * remove this test as part of the initial wiring work.
 */
```
  </action>
  <verify>
test -f src/__tests__/primitives-unused.test.ts
grep -n "DELETE THIS FILE" src/__tests__/primitives-unused.test.ts
npx vitest run primitives-unused 2>&1 | grep -E "passed|failed"
  </verify>
  <done>
Test file exists with delete-me comment. Test passes (all 5 primitives pass the "unused" assertion).
  </done>
</task>
</tasks>
<verification>
1. npm run lint -- passes with no new errors
2. npm run test -- passes (includes all 5 primitive tests + the guardrail test)
3. npm run build -- passes (TypeScript strict mode, primitives compile clean)
4. test -f src/components/Card.tsx && test -f src/components/Card.css && test -f src/__tests__/Card.test.tsx
5. test -f src/components/AppHeader.tsx && test -f src/components/AppHeader.css && test -f src/__tests__/AppHeader.test.tsx
6. test -f src/components/DegreeChipStrip.tsx && test -f src/components/DegreeChipStrip.css && test -f src/__tests__/DegreeChipStrip.test.tsx
7. test -f src/components/BottomTabBar.tsx && test -f src/components/BottomTabBar.css && test -f src/__tests__/BottomTabBar.test.tsx
8. test -f src/components/LabeledSelect.tsx && test -f src/components/LabeledSelect.css && test -f src/__tests__/LabeledSelect.test.tsx
9. test -f src/__tests__/primitives-unused.test.ts
10. Each primitive has toHaveNoViolations: grep -l "toHaveNoViolations" src/__tests__/{Card,AppHeader,DegreeChipStrip,BottomTabBar,LabeledSelect}.test.tsx | wc -l outputs 5
11. No hex colors / raw px in primitive CSS: grep -nP "#[0-9a-fA-F]{3,8}|[0-9]+px" src/components/{Card,AppHeader,DegreeChipStrip,BottomTabBar,LabeledSelect}.css | grep -v "^[^:]*:\s*0px\|transparent" outputs empty (allowance: 0px, transparent, SVG path attributes if any inline)
12. Snapshot tests (if any pre-existing) pass unchanged
</verification>
<success_criteria>
- 5 new primitives exist in src/components/ with matching .css and __tests__/*.test.tsx files
- Each primitive exports a typed functional component
- Each primitive has an axe a11y assertion in its test
- Keyboard navigation works for BottomTabBar (arrows + Home/End + roving tabindex)
- Primitives consume only tokens from tokens.css / semantic.css — no hardcoded visual values
- src/__tests__/primitives-unused.test.ts exists and passes, guaranteeing Phase 4 does not wire any primitive into existing screens
- npm run lint && npm run test && npm run build all pass
- Every existing screen renders byte-identically (Phase 4 app stability preserved)
</success_criteria>
<output>
04-05-SUMMARY.md
</output>

---

## Deviations Reconciled (R01)

### DEV-08: BottomTabBar tablist pattern

- **Original plan text (BottomTabBar implementation notes):** `<nav><ul role="tablist"><button role="tab">...`.
- **Actual delivery (commit fe04101):** `<nav><div role="tablist"><button role="tab">...`.
- **Rationale:** ARIA's `tab` role requires a `tablist` parent. Interleaving `<li>` elements between `<ul role="tablist">` and `<button role="tab">` breaks the parent-child relationship that axe's `aria-required-parent` rule enforces. The correct ARIA Authoring Practices Guide pattern for a tablist is `<div role="tablist">` containing direct `<button role="tab">` children. The `<li>` semantic layer is incompatible with the tablist pattern. The delivered code is a11y-correct; the plan text was wrong.

### DEV-09: DegreeChipStrip redundant role=list

- **Original plan text (DegreeChipStrip implementation notes):** Chips render as `<ul role="list">` with `<li>` items.
- **Actual delivery (commit fe04101):** Chips render as `<ul>` with `<li>` items (no `role="list"` attribute).
- **Rationale:** `<ul>` already has an implicit list role. Adding redundant `role="list"` is flagged by both axe (`no-redundant-roles`) and `eslint-plugin-jsx-a11y/no-redundant-roles`. Removing it does not affect screen-reader announcement and resolves the lint violation. The delivered code is correct; the plan text added a redundant attribute.
