---
phase: 4
plan: "04-02"
title: "A11y Tooling Setup"
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used:
  - ui-design:accessibility-compliance
  - vercel-react-best-practices
files_modified:
  - package.json
  - eslint.config.js
  - src/test-utils/a11y.ts
  - vitest.setup.ts
forbidden_commands: []
must_haves:
  truths:
    - "eslint-plugin-jsx-a11y is installed in warn mode for pre-existing violations and error mode for new ones — CI does not break on pre-existing violations"
    - "vitest-axe is installed and a shared test utility exposes the axe matcher"
    - "npm run lint passes after plugin installation (no new lint errors from pre-existing violations because all rules run as warn)"
    - "npm run test passes after vitest-axe setup"
    - "No product source files are modified in this plan — only config, package.json, and test infrastructure"
  artifacts:
    - path: "package.json"
      provides: "a11y tooling dependencies"
      contains: "eslint-plugin-jsx-a11y"
    - path: "package.json"
      provides: "vitest-axe dependency"
      contains: "vitest-axe"
    - path: "eslint.config.js"
      provides: "jsx-a11y plugin configured in warn mode"
      contains: "jsx-a11y"
    - path: "src/test-utils/a11y.ts"
      provides: "shared axe test helper"
      contains: "toHaveNoViolations"
    - path: "vitest.setup.ts"
      provides: "vitest-axe matchers registered globally"
      contains: "configureAxe"
  key_links:
    - from: "src/test-utils/a11y.ts"
      to: "vitest-axe"
      via: "imports and re-exports axe + matcher"
    - from: "vitest.setup.ts"
      to: "src/test-utils/a11y.ts"
      via: "imports configureAxe to register toHaveNoViolations globally"
---
<objective>
Install and configure a11y testing infrastructure that subsequent plans (04-03, 04-04) and future phases consume. Two tools are introduced:

1. eslint-plugin-jsx-a11y — lint-time ARIA/semantic checks. All rules run as "warn" so pre-existing violations surface in CI output without breaking the build. New violations introduced after this plan will also warn; Phase 5 can promote critical rules to "error" after all primitives are fixed.

2. vitest-axe — unit-test-level axe-core integration. A shared helper at src/test-utils/a11y.ts exports the configured axe function and the toHaveNoViolations matcher. Plans 04-03 and 04-04 import this helper to add axe assertions to primitive component tests.

No product source files are modified. This plan modifies only: package.json, eslint.config.js, vitest.setup.ts, and the new src/test-utils/a11y.ts.
</objective>
<context>
@.vbw-planning/phases/04-new-features-discovery-and-phase-definition/04-01-RESEARCH.md
@.vbw-planning/phases/04-new-features-discovery-and-phase-definition/.context-lead.md
</context>
<tasks>
<task type="auto">
  <name>Install eslint-plugin-jsx-a11y and vitest-axe</name>
  <files>
    package.json
  </files>
  <action>
Run the following installation command:
  npm install --save-dev eslint-plugin-jsx-a11y vitest-axe

After installation verify the packages appear in devDependencies in package.json.

Do not install @axe-core/playwright in this plan (deferred to a dedicated Playwright smoke-test phase).
  </action>
  <verify>
grep "eslint-plugin-jsx-a11y" package.json
grep "vitest-axe" package.json
  </verify>
  <done>
Both grep commands return matches showing the packages in devDependencies with version numbers.
  </done>
</task>
<task type="auto">
  <name>Configure eslint-plugin-jsx-a11y in eslint.config.js</name>
  <files>
    eslint.config.js
  </files>
  <action>
Read eslint.config.js first to understand the current config structure (it uses ESLint flat config format with @eslint/js, typescript-eslint, reactHooks, reactRefresh plugins).

Add jsx-a11y to the flat config:

1. Import the plugin at the top:
   import jsxA11y from 'eslint-plugin-jsx-a11y';

2. Add a new config object in the exported array that applies jsx-a11y rules to all TSX/TSX files. Use the "recommended" rule set but override every rule to "warn" so pre-existing violations don't break CI:

   {
     files: ['**/*.{ts,tsx}'],
     plugins: { 'jsx-a11y': jsxA11y },
     rules: {
       // All jsx-a11y recommended rules set to warn so pre-existing violations
       // surface without breaking CI. Promote to 'error' in Phase 5 after all
       // primitives are fixed. See: Phase 4 Plan 04-02.
       ...Object.fromEntries(
         Object.keys(jsxA11y.configs.recommended.rules).map(rule => [rule, 'warn'])
       ),
     },
   }

Add a comment above this block:
  // jsx-a11y: accessibility lint rules — all warn in Phase 4 (pre-existing violations).
  // Promote critical rules to 'error' in Phase 5 after primitive a11y work completes.

After editing, run npm run lint and confirm it passes (warnings are acceptable, errors are not).
  </action>
  <verify>
grep -n "jsx-a11y" eslint.config.js
npm run lint 2>&1 | tail -5
  </verify>
  <done>
grep returns the plugin import and config block. npm run lint exits 0 (warnings are ok, errors are not).
  </done>
</task>
<task type="auto">
  <name>Create shared a11y test utility</name>
  <files>
    src/test-utils/a11y.ts
  </files>
  <action>
Check if src/test-utils/ directory exists. If not, create it.

Create src/test-utils/a11y.ts with this content:

/**
 * Shared a11y test utility for vitest-axe.
 *
 * Usage in component test files:
 *   import { axe } from '../test-utils/a11y';
 *   it('has no a11y violations', async () => {
 *     const { container } = render(<MyComponent />);
 *     expect(await axe(container)).toHaveNoViolations();
 *   });
 *
 * The toHaveNoViolations matcher is registered globally in vitest.setup.ts.
 */
import { configureAxe } from 'vitest-axe';

export const axe = configureAxe({
  rules: {
    // color-contrast: disabled in unit tests — axe cannot measure CSS custom property
    // values in jsdom (computed styles are not available). Contrast must be checked
    // via browser-based tools or @axe-core/playwright integration.
    'color-contrast': { enabled: false },
  },
});

export { configureAxe };
  </action>
  <verify>
cat src/test-utils/a11y.ts | grep "configureAxe"
cat src/test-utils/a11y.ts | grep "toHaveNoViolations"
  </verify>
  <done>
File exists at src/test-utils/a11y.ts. First grep finds configureAxe import. Second grep finds the toHaveNoViolations reference in JSDoc. File compiles (npm run build passes).
  </done>
</task>
<task type="auto">
  <name>Register vitest-axe matchers in vitest.setup.ts</name>
  <files>
    vitest.setup.ts
  </files>
  <action>
Read vitest.setup.ts first to understand the current setup (it likely imports @testing-library/jest-dom).

Add the following import at the top or after existing imports:
  import { configureAxe } from 'vitest-axe';
  import 'vitest-axe/extend-expect';

Then call configureAxe globally for the same default settings as the shared utility:
  configureAxe({
    rules: {
      'color-contrast': { enabled: false },
    },
  });

Add a comment:
  // vitest-axe: registers toHaveNoViolations matcher globally.
  // color-contrast rule disabled — jsdom cannot compute CSS custom property values.
  // Use @axe-core/playwright for contrast checks in a real browser environment.

After editing, run npm run test and confirm all tests still pass.
  </action>
  <verify>
grep -n "vitest-axe" vitest.setup.ts
npm run test 2>&1 | tail -10
  </verify>
  <done>
grep returns the import line. npm run test exits 0 with all tests passing.
  </done>
</task>
</tasks>
<verification>
1. npm install succeeded -- grep "eslint-plugin-jsx-a11y" package.json returns a version string
2. npm install succeeded -- grep "vitest-axe" package.json returns a version string
3. npm run lint -- exits 0 (lint passes; warnings about pre-existing a11y issues are acceptable)
4. npm run test -- exits 0 (all existing tests pass)
5. npm run build -- exits 0
6. src/test-utils/a11y.ts exists and exports axe function
7. vitest.setup.ts imports vitest-axe and registers toHaveNoViolations globally
</verification>
<success_criteria>
- eslint-plugin-jsx-a11y installed and configured in warn mode in eslint.config.js
- vitest-axe installed and toHaveNoViolations matcher available globally in all test files
- src/test-utils/a11y.ts exports axe() configured with color-contrast disabled (jsdom limitation)
- npm run lint, npm run test, npm run build all pass with no regressions
- No product source files were modified
</success_criteria>
<output>
04-02-SUMMARY.md
</output>

---

## Deviations Reconciled (R01)

### DEV-02: vitest-axe matcher import path

- **Original plan text (implied via test setup intent):** Register the vitest-axe `toHaveNoViolations` matcher via `import { toHaveNoViolations } from 'vitest-axe/extend-expect'` or similar.
- **Actual delivery (commit 86f258d):** Registered via direct `expect.extend` using the named export from `vitest-axe/matchers` (the `dist/matchers.js` path). The `vitest-axe/extend-expect` module is a non-functional empty file in vitest-axe v0.1.0.
- **Rationale:** The intended public API of vitest-axe v0.1.0 does not expose `extend-expect` as a working path. The `matchers` export is the canonical extension point. Functionality is identical; the import path is the only difference.
- **Follow-up:** When vitest-axe releases a version with a working `extend-expect` module, swap back to the single-import pattern for a tighter setup.ts.
